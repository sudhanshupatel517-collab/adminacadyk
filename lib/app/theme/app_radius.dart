import 'package:flutter/material.dart';

/// Acadyk Admin — Border Radius Scale.
/// Enterprise aesthetic: structured, not pill-shaped.
class AppRadius {
  static const double xs = 3.0;
  static const double sm = 4.0;
  static const double md = 6.0;
  static const double lg = 8.0;

  static const BorderRadius roundedXs = BorderRadius.all(Radius.circular(3.0));
  static const BorderRadius roundedSm = BorderRadius.all(Radius.circular(4.0));
  static const BorderRadius roundedMd = BorderRadius.all(Radius.circular(6.0));
  static const BorderRadius roundedLg = BorderRadius.all(Radius.circular(8.0));
}
